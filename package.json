{
  "name": "jsonspecs-cli",
  "version": "1.1.0",
  "description": "CLI backend and local studio host for jsonspecs rules projects",
  "type": "commonjs",
  "bin": {
    "jsonspecs": "bin/jsonspecs.js"
  },
  "main": "index.js",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "express": "^5.2.1",
    "jsonspecs": "^1.1.0"
  }
}
